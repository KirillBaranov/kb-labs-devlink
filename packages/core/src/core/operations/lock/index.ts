export { 
  freezeToLock, 
  freezeToLockMerged, 
  type LockFile, 
  type LockMeta,
  type LockConsumer,
  type LockEntry, 
  type SourceHint,
  type LockUpdates,
  type FreezeDryRunResult, 
  type FreezeMergeOptions 
} from "./freeze";
export { applyLock } from "./apply-lock";

